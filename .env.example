# Leave both blank to run on local sample listings.
# When set, the app reads and writes the public.listings table.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Used in confirmation emails. Local default is the reserved app port.
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:47281
